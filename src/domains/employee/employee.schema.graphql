export const typeDefs = `#graphql
scalar Date


type Employee {
    id: ID!
    firstName: String!
    lastName: String!
    role: String!
    email:String!
    phoneNumber: String!
    timeEntries: [TimeEntry]!
}


#Entry points
type Query {
    employees: [Employee]!
    employee(id: ID!): [TimeEntry]!
}

`
