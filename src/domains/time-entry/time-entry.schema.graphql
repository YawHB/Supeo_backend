scalar Date
scalar Time
scalar Unix

extend type Query {
  timeEntries: [TimeEntry]!
  timeEntry(id: ID!): TimeEntry!
}

type Mutation {
  createTimeEntry(newTimeEntry: CreateTimeEntryInput): TimeEntry!
}

extend type Mutation {
  updateTimeEntryStatus(notification: NotificationStatusInput!): Notification!
}
