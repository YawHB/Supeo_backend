scalar Date
scalar Unix
scalar Time

type Query {
  employee(id: ID!): Employee!
  #employees: [Employee]!
  employees(sort: SortInput, filter: EmployeeFilterInput): [Employee]!
  # employees(
  #   orderBy: String
  #   order: OrderDirection
  #   employeeRole: [String!]
  #   employeePermission: [String!]
  # ): [Employee!]!

  #employees(pagination: PaginationInput): EmployeesPage!

  notification(id: ID!): Notification!
  notifications: [Notification]!

  timeEntry(id: ID!): TimeEntry!
  timeEntries: [TimeEntry]!

  roles: [Role]!
  permissions: [Permission]!

  userMenuLinks(employeeId: Int!): [UserMenuLink!]!
}
